% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mclp.R
\name{mclp}
\alias{mclp}
\title{Maximal Covering Location Problem (MCLP)}
\usage{
mclp(
  demand,
  demand_id,
  demand_weight = NULL,
  candidate,
  candidate_id,
  candidate_weight = NULL,
  existing_sites = NULL,
  existing_sites_id = NULL,
  existing_sites_weight = NULL,
  matrix_OD_candidates,
  matrix_OD_candidates_from_id = "from_id",
  matrix_OD_candidates_to_id = "to_id",
  matrix_OD_candidates_dist = "distance",
  matrix_OD_existing_site = NULL,
  matrix_OD_existing_site_from_id = NULL,
  matrix_OD_existing_site_to_id = NULL,
  matrix_OD_existing_site_dist = NULL,
  cutoff_distance = 1000,
  service_radius,
  p_facilities,
  solver = "glpk"
)
}
\arguments{
\item{demand}{sf POINT. Demand points.}

\item{demand_id}{character. Unique id column in \code{demand}.}

\item{demand_weight}{character or NULL. Weight column in \code{demand} (e.g.
population). This is the primary driver of MCLP's objective --
candidates are chosen to maximize total weighted covered demand.
Defaults to 1 if NULL.}

\item{candidate}{sf POINT. Candidate facility sites.}

\item{candidate_id}{character. Unique id column in \code{candidate}.}

\item{candidate_weight}{character or NULL. Unused by LSCP.}

\item{existing_sites}{sf POINT or NULL. Facilities already open, forced
into the solution.}

\item{existing_sites_id}{character or NULL.}

\item{existing_sites_weight}{character or NULL. Unused by LSCP.}

\item{matrix_OD_candidates}{data.frame. Long OD table demand-to-candidate.}

\item{matrix_OD_candidates_from_id}{character.}

\item{matrix_OD_candidates_to_id}{character.}

\item{matrix_OD_candidates_dist}{character.}

\item{matrix_OD_existing_site}{data.frame or NULL.}

\item{matrix_OD_existing_site_from_id}{character or NULL.}

\item{matrix_OD_existing_site_to_id}{character or NULL.}

\item{matrix_OD_existing_site_dist}{character or NULL.}

\item{cutoff_distance}{numeric. Pairs beyond this distance are dropped.}

\item{service_radius}{numeric. Maximum acceptable distance.}

\item{p_facilities}{integer. Number of \emph{new} facilities to open (the
budget applies only to \code{candidate}, not to forced-open
\code{existing_sites}).}

\item{solver}{character. ROI solver, default \code{"glpk"}.}
}
\value{
An object of class \code{llocalocal_result}.
}
\description{
Selects up to \code{p_facilities} candidate sites to maximize the weighted
demand covered within \code{service_radius}. Unlike \code{\link[=lscp]{lscp()}}, full coverage
of every demand point is not required -- demand that can't be reached
by any candidate within the radius is simply left uncovered, which is
the point of "maximal" coverage rather than total coverage.
\code{existing_sites}, if supplied, are treated as already open: they
contribute coverage for free and don't consume the \code{p_facilities}
budget (same Required Facilities semantics as \code{\link[=lscp]{lscp()}}/\code{\link[=p_median]{p_median()}}/
\code{\link[=p_center]{p_center()}}).
}

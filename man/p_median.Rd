% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/p_median.R
\name{p_median}
\alias{p_median}
\title{P-Median Problem}
\usage{
p_median(
  candidate,
  candidate_id,
  candidate_weight = NULL,
  existing_sites = NULL,
  existing_sites_id = NULL,
  existing_sites_weight = NULL,
  demand,
  demand_id,
  demand_weight = NULL,
  matrix_OD_candidates,
  matrix_OD_candidates_from_id = "from_id",
  matrix_OD_candidates_to_id = "to_id",
  matrix_OD_candidates_dist = "distance",
  matrix_OD_existing_site = NULL,
  matrix_OD_existing_site_from_id = NULL,
  matrix_OD_existing_site_to_id = NULL,
  matrix_OD_existing_site_dist = NULL,
  cutoff_distance = 1000,
  p_facilities,
  solver = "glpk"
)
}
\arguments{
\item{candidate}{sf POINT. Sites candidats à évaluer.}

\item{candidate_id}{character. Colonne d'identifiant unique dans
\code{candidate}.}

\item{candidate_weight}{character ou NULL. Colonne de poids dans
\code{candidate}. Si NULL, poids = 1.}

\item{existing_sites}{sf POINT ou NULL. Sites déjà ouverts,
forcés dans la solution (Required Facilities).}

\item{existing_sites_id}{character ou NULL. Colonne d'identifiant
unique dans \code{existing_sites}.}

\item{existing_sites_weight}{character ou NULL. Colonne de poids dans
\code{existing_sites}. Si NULL, poids = 1.}

\item{demand}{sf POINT. Points de demande (clients).}

\item{demand_id}{character. Colonne d'identifiant unique dans
\code{demand}.}

\item{demand_weight}{character ou NULL. Colonne de poids (population,
volume…) dans \code{demand}. Si NULL, poids = 1.}

\item{matrix_OD_candidates}{data.frame. Matrice OD longue entre
demande et candidates.}

\item{matrix_OD_candidates_from_id}{character. Colonne origine (id demande).}

\item{matrix_OD_candidates_to_id}{character. Colonne destination (id candidat).}

\item{matrix_OD_candidates_dist}{character. Colonne distance.}

\item{matrix_OD_existing_site}{data.frame ou NULL. Matrice OD longue
entre demande et sites existants.
Obligatoire si \code{existing_sites}
est fourni.}

\item{matrix_OD_existing_site_from_id}{character ou NULL. Colonne origine.}

\item{matrix_OD_existing_site_to_id}{character ou NULL. Colonne destination.}

\item{matrix_OD_existing_site_dist}{character ou NULL. Colonne distance.}

\item{cutoff_distance}{numeric. Distance maximale au-delà de laquelle une
paire demande-facility est ignorée (élagage du modèle).}

\item{p_facilities}{integer. Nombre de facilities à ouvrir.}

\item{solver}{character. Solveur ROI : \code{"glpk"} (défaut)}
}
\value{
Une liste de classe \code{localalloc_result}.
}
\description{
Sélectionne exactement le nombre facilities pour
\strong{minimiser la distance totale pondérée} entre chaque client
et sa facility la plus proche.
Les sites existants (\code{existing_sites}) sont  ouverts dans le ILP
dès le départ.
}

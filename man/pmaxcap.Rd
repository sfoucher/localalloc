% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pmaxcap.R
\name{pmaxcap}
\alias{pmaxcap}
\title{Maximum Capture Problem with Price (PMAXCAP)}
\usage{
pmaxcap(
  demand,
  demand_id,
  demand_weight = NULL,
  candidate,
  candidate_id,
  existing_sites,
  existing_sites_id,
  matrix_OD_candidates,
  matrix_OD_candidates_from_id = "from_id",
  matrix_OD_candidates_to_id = "to_id",
  matrix_OD_candidates_dist = "distance",
  matrix_OD_existing_site,
  matrix_OD_existing_site_from_id = "from_id",
  matrix_OD_existing_site_to_id = "to_id",
  matrix_OD_existing_site_dist = "distance",
  cutoff_distance = NULL,
  marginal_cost = 0,
  distance_cost_rate = 1,
  competitor_price,
  n_facilities,
  candidate_fixed_cost = NULL,
  max_breakpoints = 2000,
  solver = "highs"
)
}
\arguments{
\item{demand}{sf POINT. Demand points.}

\item{demand_id}{character. Unique id column in \code{demand}.}

\item{demand_weight}{character or NULL. Weight column in \code{demand} (e.g.
population). This drives the objective -- captured demand is weighted
by this column. Defaults to 1 if NULL.}

\item{candidate}{sf POINT. Candidate facility sites.}

\item{candidate_id}{character. Unique id column in \code{candidate}.}

\item{existing_sites}{sf POINT. The competitor's sites (required).}

\item{existing_sites_id}{character. Unique id column in \code{existing_sites}.}

\item{matrix_OD_candidates}{data.frame. Long OD table demand-to-candidate.}

\item{matrix_OD_candidates_from_id}{character.}

\item{matrix_OD_candidates_to_id}{character.}

\item{matrix_OD_candidates_dist}{character.}

\item{matrix_OD_existing_site}{data.frame. Required.}

\item{matrix_OD_existing_site_from_id}{character or NULL.}

\item{matrix_OD_existing_site_to_id}{character or NULL.}

\item{matrix_OD_existing_site_dist}{character or NULL.}

\item{cutoff_distance}{numeric or NULL. Pairs beyond this distance are
dropped. \code{NULL} (default) means no cutoff.}

\item{marginal_cost}{numeric. Marginal production cost per unit (v).}

\item{distance_cost_rate}{numeric. Cost per unit distance (t).}

\item{competitor_price}{numeric. The competitor's fixed price (P_B).}

\item{n_facilities}{integer. Exact number of new facilities to open (n_A).}

\item{candidate_fixed_cost}{character or NULL. Column in \code{candidate} for
the fixed cost of opening each site (f_j, eq. 2.36). Defaults to 0 if
NULL.}

\item{max_breakpoints}{integer. Caps the number of price breakpoints
evaluated. Under the cap: exact. Over it: breakpoints are subsampled
and a warning is raised -- the result is then an approximation, not
the exact global optimum. Default 2000.}

\item{solver}{character. \code{"highs"} (default) or \code{"glpk"}.}
}
\value{
An object of class \code{localocal_result}, with \code{optimal_price} and
\code{profit} fields in addition to the usual ones.
}
\description{
Extends \code{\link[=maxcap]{maxcap()}} by making the firm's price a decision variable. For
a fixed set of open sites, profit is piecewise-linear in price (capture
only flips at threshold breakpoints), so the true global optimum is
found by enumerating every breakpoint price, solving the resulting
MAXCAP-style linear MIP at each one, and keeping the best. This needs
no nonlinear/MINLP solver.
}
\details{
\deqn{\text{Maximize } \Pi = (P^A - v) \sum_{i \in I} a_i Y_i^A - \sum_{j \in J} f_j X_j^A}
\deqn{\text{s.t. } Y_i^A \leq \sum_{j \in N_i(b_i^B)} X_j^A, \; \forall i \qquad \sum_{j=1}^{m} X_j^A = n^A}
\deqn{X_j^A, Y_i^A \in \{0,1\}}
where \eqn{N_i(b_i^B) = \{j \in J : P^A + t\,d_{ij} < P^B + t\,d_{i,b_i^B}\}}
(capture zone, depends on price \eqn{P^A}), \eqn{P^A} = optimized
price (returned in \code{optimal_price}), \eqn{v} = \code{marginal_cost},
\eqn{f_j} = \code{candidate_fixed_cost}, \eqn{n^A} = \code{n_facilities},
\eqn{P^B} = \code{competitor_price}, \eqn{t} = \code{distance_cost_rate}. Solved
by enumerating price breakpoints (see description above), not directly
as a MINLP.
}
